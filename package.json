{
  "private": true,
  "scripts": {
    "ready": "vp fmt && vp run -r build && vp lint && vp run -r test",
    "build": "vp run -r build",
    "test": "vp run -r test",
    "prepare": "vp config",
    "format": "vp fmt"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": "26.10.0",
      "onFail": "download"
    }
  },
  "packageManager": "pnpm@12.5.1"
}
